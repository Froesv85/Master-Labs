services:
  mysql:
    image: mysql:8.0
    container_name: maker-mysql
    ports:
      - "3307:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: maker
    volumes:
      - mysql-data:/var/lib/mysql

volumes:
  mysql-data:
